#!/usr/bin/env bash
set -e

# SageMaker запускає контейнер як: docker run <image> serve
# тому тут просто піднімаємо веб-сервер інференсу
exec gunicorn -b 0.0.0.0:8080 app:app --workers 1 --threads 4 --timeout 60
